@model ITAssetsDatabase.Models.KnowledgeBase.TestUploadViewModel

@{
    ViewBag.Title = "TestUpload";
}


<style>

	input {

	 width: 800px;
		}

</style>



<h2>TestUpload</h2>

@using (Html.BeginForm("TestUpload", "KnowledgeBase", FormMethod.Post, new { enctype = "multipart/form-data", id = "thisForm" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TestUploadViewModel</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.serverFilepath)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.serverFilepath)
            @Html.ValidationMessageFor(model => model.serverFilepath)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.filename)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.filename)
            @Html.ValidationMessageFor(model => model.filename)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.fullpath)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.fullpath)
            @Html.ValidationMessageFor(model => model.fullpath)
        </div>

        <div class="editor-field">
                       

            <input class="upload" type="file" name="fileupload_object" id="file1" />

        </div>


            <p>
                <input type="submit" value="Create" />
            </p>
</fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
